---

- name: Create keystore
  ansible.builtin.command: /usr/share/elasticsearch/bin/elasticsearch-keystore create
  args:
    creates: /etc/elasticsearch/elasticsearch.keystore

- name: Check for bootstrap password
  ansible.builtin.command: /usr/share/elasticsearch/bin/elasticsearch-keystore list
  changed_when: false
  register: elasticsearch_keystore

- name: Remove autoconfiguration password hash
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    remove autoconfiguration.password_hash
  when: "'autoconfiguration.password_hash' in elasticsearch_keystore.stdout_lines"
  changed_when: true
  notify:
    - Restart Elasticsearch

- name: Set bootstrap password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    add -x -f 'bootstrap.password'
  args:
    stdin: "{{ elasticsearch_bootstrap_pw }}"
  changed_when: false
  no_log: true
  notify:
    - Restart Elasticsearch
  ignore_errors: "{{ ansible_check_mode }}"

# --- HTTP SSL keystore/truststore passwords ---
# For auto-generated certs (P12): always set passwords from elasticsearch_tls_key_passphrase
# For external P12: set passwords from http passphrase variable
# For external PEM: skip entirely (no keystore needed)

- name: Get xpack.security.http.ssl.keystore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    show xpack.security.http.ssl.keystore.secure_password
  when:
    - "'xpack.security.http.ssl.keystore.secure_password' in elasticsearch_keystore.stdout_lines"
    - elasticsearch_http_security
    - elasticsearch_cert_source == 'elasticsearch_ca' or (_elasticsearch_http_cert_format | default('')) == 'p12'
  register: elasticsearch_http_ssl_keystore_secure_password
  ignore_errors: "{{ ansible_check_mode }}"
  no_log: true
  changed_when: false

- name: Set xpack.security.http.ssl.keystore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    add -f -x 'xpack.security.http.ssl.keystore.secure_password'
  args:
    stdin: >-
      {{ (elasticsearch_http_tls_key_passphrase | default(elasticsearch_transport_tls_key_passphrase, true))
         if elasticsearch_cert_source == 'external'
         else elasticsearch_tls_key_passphrase }}
  changed_when: false
  no_log: true
  when:
    - elasticsearch_http_security
    - elasticsearch_cert_source == 'elasticsearch_ca' or (_elasticsearch_http_cert_format | default('')) == 'p12'
    - >-
      elasticsearch_http_ssl_keystore_secure_password.stdout is undefined or
      ((elasticsearch_http_tls_key_passphrase | default(elasticsearch_transport_tls_key_passphrase, true))
       if elasticsearch_cert_source == 'external'
       else elasticsearch_tls_key_passphrase) != elasticsearch_http_ssl_keystore_secure_password.stdout
  notify:
    - Restart Elasticsearch

- name: Remove xpack.security.http.ssl.keystore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    remove xpack.security.http.ssl.keystore.secure_password
  changed_when: false
  no_log: true
  when:
    - "'xpack.security.http.ssl.keystore.secure_password' in elasticsearch_keystore.stdout_lines"
    - not elasticsearch_http_security or (elasticsearch_cert_source == 'external' and (_elasticsearch_http_cert_format | default('')) == 'pem')
  notify:
    - Restart Elasticsearch

- name: Get xpack.security.http.ssl.truststore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    show xpack.security.http.ssl.truststore.secure_password
  when:
    - "'xpack.security.http.ssl.truststore.secure_password' in elasticsearch_keystore.stdout_lines"
    - elasticsearch_http_security
    - elasticsearch_cert_source == 'elasticsearch_ca' or (_elasticsearch_http_cert_format | default('')) == 'p12'
  register: elasticsearch_http_ssl_truststore_secure_password
  ignore_errors: "{{ ansible_check_mode }}"
  no_log: true
  changed_when: false

- name: Set xpack.security.http.ssl.truststore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    add -f -x 'xpack.security.http.ssl.truststore.secure_password'
  args:
    stdin: >-
      {{ (elasticsearch_http_tls_key_passphrase | default(elasticsearch_transport_tls_key_passphrase, true))
         if elasticsearch_cert_source == 'external'
         else elasticsearch_tls_key_passphrase }}
  changed_when: false
  no_log: true
  when:
    - elasticsearch_http_security
    - elasticsearch_cert_source == 'elasticsearch_ca' or (_elasticsearch_http_cert_format | default('')) == 'p12'
    - >-
      elasticsearch_http_ssl_truststore_secure_password.stdout is undefined or
      ((elasticsearch_http_tls_key_passphrase | default(elasticsearch_transport_tls_key_passphrase, true))
       if elasticsearch_cert_source == 'external'
       else elasticsearch_tls_key_passphrase) != elasticsearch_http_ssl_truststore_secure_password.stdout
  notify:
    - Restart Elasticsearch

- name: Remove xpack.security.http.ssl.truststore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    remove xpack.security.http.ssl.truststore.secure_password
  changed_when: false
  no_log: true
  when:
    - "'xpack.security.http.ssl.truststore.secure_password' in elasticsearch_keystore.stdout_lines"
    - not elasticsearch_http_security or (elasticsearch_cert_source == 'external' and (_elasticsearch_http_cert_format | default('')) == 'pem')
  notify:
    - Restart Elasticsearch

# --- Transport SSL keystore/truststore passwords ---

- name: Get xpack.security.transport.ssl.keystore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    show xpack.security.transport.ssl.keystore.secure_password
  when:
    - "'xpack.security.transport.ssl.keystore.secure_password' in elasticsearch_keystore.stdout_lines"
    - elasticsearch_security
    - elasticsearch_cert_source == 'elasticsearch_ca' or (_elasticsearch_transport_cert_format | default('')) == 'p12'
  register: elasticsearch_transport_ssl_keystore_secure_password
  ignore_errors: "{{ ansible_check_mode }}"
  no_log: true
  changed_when: false

- name: Set xpack.security.transport.ssl.keystore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    add -f -x 'xpack.security.transport.ssl.keystore.secure_password'
  args:
    stdin: >-
      {{ elasticsearch_transport_tls_key_passphrase
         if elasticsearch_cert_source == 'external'
         else elasticsearch_tls_key_passphrase }}
  changed_when: false
  no_log: true
  when:
    - elasticsearch_security
    - elasticsearch_cert_source == 'elasticsearch_ca' or (_elasticsearch_transport_cert_format | default('')) == 'p12'
    - >-
      elasticsearch_transport_ssl_keystore_secure_password.stdout is undefined or
      (elasticsearch_transport_tls_key_passphrase
       if elasticsearch_cert_source == 'external'
       else elasticsearch_tls_key_passphrase) != elasticsearch_transport_ssl_keystore_secure_password.stdout
  notify:
    - Restart Elasticsearch

- name: Remove xpack.security.transport.ssl.keystore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    remove xpack.security.transport.ssl.keystore.secure_password
  changed_when: false
  no_log: true
  when:
    - "'xpack.security.transport.ssl.keystore.secure_password' in elasticsearch_keystore.stdout_lines"
    - not elasticsearch_security or (elasticsearch_cert_source == 'external' and (_elasticsearch_transport_cert_format | default('')) == 'pem')
  notify:
    - Restart Elasticsearch

- name: Get xpack.security.transport.ssl.truststore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    show xpack.security.transport.ssl.truststore.secure_password
  when:
    - "'xpack.security.transport.ssl.truststore.secure_password' in elasticsearch_keystore.stdout_lines"
    - elasticsearch_security
    - elasticsearch_cert_source == 'elasticsearch_ca' or (_elasticsearch_transport_cert_format | default('')) == 'p12'
  register: elasticsearch_transport_ssl_truststore_secure_password
  ignore_errors: "{{ ansible_check_mode }}"
  no_log: true
  changed_when: false

- name: Set xpack.security.transport.ssl.truststore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    add -f -x 'xpack.security.transport.ssl.truststore.secure_password'
  args:
    stdin: >-
      {{ elasticsearch_transport_tls_key_passphrase
         if elasticsearch_cert_source == 'external'
         else elasticsearch_tls_key_passphrase }}
  changed_when: false
  no_log: true
  when:
    - elasticsearch_security
    - elasticsearch_cert_source == 'elasticsearch_ca' or (_elasticsearch_transport_cert_format | default('')) == 'p12'
    - >-
      elasticsearch_transport_ssl_truststore_secure_password.stdout is undefined or
      (elasticsearch_transport_tls_key_passphrase
       if elasticsearch_cert_source == 'external'
       else elasticsearch_tls_key_passphrase) != elasticsearch_transport_ssl_truststore_secure_password.stdout
  notify:
    - Restart Elasticsearch

- name: Remove xpack.security.transport.ssl.truststore.secure_password
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    remove xpack.security.transport.ssl.truststore.secure_password
  changed_when: false
  no_log: true
  when:
    - "'xpack.security.transport.ssl.truststore.secure_password' in elasticsearch_keystore.stdout_lines"
    - not elasticsearch_security or (elasticsearch_cert_source == 'external' and (_elasticsearch_transport_cert_format | default('')) == 'pem')
  notify:
    - Restart Elasticsearch

# --- PEM key passphrase in keystore for external PEM certs ---

- name: Set xpack.security.transport.ssl.secure_key_passphrase
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    add -f -x 'xpack.security.transport.ssl.secure_key_passphrase'
  args:
    stdin: "{{ elasticsearch_transport_tls_key_passphrase }}"
  changed_when: false
  no_log: true
  when:
    - elasticsearch_cert_source == 'external'
    - (_elasticsearch_transport_cert_format | default('')) == 'pem'
    - elasticsearch_transport_tls_key_passphrase | length > 0
  notify:
    - Restart Elasticsearch

- name: Set xpack.security.http.ssl.secure_key_passphrase
  ansible.builtin.command: >
    /usr/share/elasticsearch/bin/elasticsearch-keystore
    add -f -x 'xpack.security.http.ssl.secure_key_passphrase'
  args:
    stdin: "{{ elasticsearch_http_tls_key_passphrase | default(elasticsearch_transport_tls_key_passphrase, true) }}"
  changed_when: false
  no_log: true
  when:
    - elasticsearch_cert_source == 'external'
    - (_elasticsearch_http_cert_format | default('')) == 'pem'
    - (elasticsearch_http_tls_key_passphrase | default(elasticsearch_transport_tls_key_passphrase, true)) | length > 0
    - elasticsearch_http_security
  notify:
    - Restart Elasticsearch

- name: Ensure keystore permissions
  ansible.builtin.file:
    path: /etc/elasticsearch/elasticsearch.keystore
    owner: root
    group: elasticsearch
    mode: "0660"
