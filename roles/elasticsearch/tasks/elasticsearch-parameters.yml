---
- name: Warn when security is disabled
  ansible.builtin.debug:
    msg: >-
      Elasticsearch security is disabled. TLS, authentication, and RBAC will not be configured.
      This is not recommended for production use.
  when:
    - not elasticsearch_security | default(true) | bool
