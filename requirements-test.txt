# Keep the shared test environment on the collection's declared minimum
# ansible-core minor release. Newer supported releases are tested separately
# by the explicit CI matrices.
ansible-core>=2.20,<2.21
ansible-lint>=26.8,<27
molecule>=26.8.0,<27
pytest>=9.1.1,<10
passlib==1.7.4
